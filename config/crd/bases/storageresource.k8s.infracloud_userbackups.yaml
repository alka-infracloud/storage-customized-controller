---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: userbackups.storageresource.k8s.infracloud
spec:
  group: storageresource.k8s.infracloud
  names:
    kind: UserBackup
    listKind: UserBackupList
    plural: userbackups
    singular: userbackup
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: UserBackup is the Schema for the userbackups API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: UserBackupSpec defines the desired state of UserBackup.
            properties:
              pvcName:
                type: string
              snapshotClassName:
                type: string
            required:
            - pvcName
            - snapshotClassName
            type: object
          status:
            description: UserBackupStatus defines the observed state of UserBackup.
            properties:
              conditions:
                items:
                  description: UserBackupCondition describes the state of a userbackup
                    object at a certain point.
                  properties:
                    lastTransitionTime:
                      description: The last time the condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: A human readable message indicating details about
                        the transition.
                      type: string
                    reason:
                      description: The reason for the condition's last transition.
                      type: string
                    status:
                      description: Status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              pvcAccessMode:
                type: string
              pvcStorageClassName:
                type: string
              restoreSize:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
